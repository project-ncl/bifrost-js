{
  "private": true,
  "name": "@project-ncl/bifrost-js-root",
  "version": "0.1.1",
  "repository": {
    "type": "git",
    "url": "https://github.com/project-ncl/bifrost-js.git"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "bootstrap:ci": "lerna bootstrap --ci",
    "build": "lerna run build --stream",
    "lint": "lerna run lint --stream",
    "test": "lerna run test --stream"
  },
  "devDependencies": {
    "@types/node": "^12.12.6",
    "lerna": "^3.18.4",
    "lerna-audit": "^1.3.0",
    "ts-lint": "^4.5.1",
    "typescript": "^3.7.2"
  }
}
