{
  "private": true,
  "name": "@project-ncl/bifrost-js-root",
  "version": "0.1.4",
  "repository": {
    "type": "git",
    "url": "https://github.com/project-ncl/bifrost-js.git"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "bootstrap:ci": "lerna bootstrap --ci",
    "build": "lerna bootstrap --hoist && lerna run build --stream && lerna run test --stream",
    "lint": "lerna run lint --stream",
    "test": "lerna run test --stream"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "lerna": "^6.0.3",
    "lerna-audit": "^1.3.3",
    "ts-lint": "^4.5.1",
    "typescript": "^4.9.3"
  },
  "engines": {
    "node": "^18.12.0",
    "npm": "^8.19.0"
  }
}
